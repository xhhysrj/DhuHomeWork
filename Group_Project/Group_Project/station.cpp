#pragma once
#include "station.h"
#include<vector>
#include<string>
using namespace std;
Station::Station(int id, string name, string line, string status):id(id),name(name),line(line),status(status) {}